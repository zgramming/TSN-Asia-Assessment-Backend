
# Example 1: Local PC Run (PostgreSQL installed on your computer)
PORT=5000
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=tsn-assessment
NODE_ENV=development

# Example 2: Local Docker Run (using Docker Compose, DB_HOST should match service name in docker-compose.yml)
# Uncomment below and comment out above if using Docker Compose
# PORT=5000
# DB_HOST=db
# DB_PORT=5432
# DB_USER=postgres
# DB_PASS=postgres
# DB_NAME=tsn-assessment
# NODE_ENV=development
